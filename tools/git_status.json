{
  "type": "function",
  "function": {
    "name": "git_status",
    "description": "Show git status for a repository",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "description": "The repository path to check",
          "type": "string"
        }
      },
      "required": [
        "path"
      ]
    }
  },
  "bash_template": "git status {{{path}}}"
}