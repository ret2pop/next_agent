{
  "type": "function",
  "agent_instruction": "Use this to search the user's PKM database, which includes their agenda, journal, configurations, and everything about them.",
  "function": {
    "name": "monorepo_query",
    "description": "Search the monorepo codebase for specific context about the user, and about the NixOS flake codebase.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The exact search query to look up in the vector database."
        }
      },
      "required": [
        "query"
      ]
    }
  }
}
